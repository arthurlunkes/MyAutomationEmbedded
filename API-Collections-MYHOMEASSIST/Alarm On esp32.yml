info:
  name: Alarm On esp32
  type: http
  seq: 8

http:
  method: GET
  url: "{{URL-BASE}}/H"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: example success
    request:
      url: "{{URL-BASE}}/H"
      method: GET
    response:
      status: 200
      statusText: OK
      headers:
        - name: content-type
          value: application/json
        - name: content-length
          value: "16"
        - name: connection
          value: close
      body:
        type: json
        data: |-
          {
            "success": true
          }
