info:
  name: Config WiFi esp32
  type: http
  seq: 10

http:
  method: POST
  url: http://192.168.4.1/wifi
  body:
    type: json
    data: |-
      {
        "ssid": "Arthur_PalmasNet",
        "password": "03957455944"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: example success
    request:
      url: http://192.168.4.1/wifi
      method: POST
      body:
        type: json
        data: |-
          {
            "ssid": "Arthur_PalmasNet",
            "password": "03957455944"
          }
    response:
      status: 200
      statusText: OK
      headers:
        - name: content-type
          value: application/json
        - name: content-length
          value: "16"
        - name: connection
          value: close
      body:
        type: json
        data: |-
          {
            "success": true
          }
