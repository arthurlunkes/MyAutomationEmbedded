info:
  name: Humidity esp32
  type: http
  seq: 4

http:
  method: GET
  url: "{{URL-BASE}}/humidity"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: example success
    request:
      url: "{{URL-BASE}}/humidity"
      method: GET
    response:
      status: 200
      statusText: OK
      headers:
        - name: content-type
          value: application/json
        - name: content-length
          value: "26"
        - name: connection
          value: close
      body:
        type: json
        data: |-
          {
            "humidity": 60,
            "unit": "%"
          }
